apiVersion: v1
kind: Secret
metadata:
  name: mtto-secrets
  namespace: mtto-system
  labels:
    app: mtto-pro
type: Opaque
stringData:
  # MySQL Root Password
  MYSQL_ROOT_PASSWORD: "rootpassword123"
  
  # MySQL Application User
  MYSQL_USER: "mtto_user"
  MYSQL_PASSWORD: "mtto_password"
  
  # JWT Secret (for future auth)
  JWT_SECRET: "mtto-super-secret-key-2024"

